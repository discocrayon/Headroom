# Output Examples

Illustrative output from one run. This document is informative — the normative
descriptions of these artifacts live in
[`spec/contracts/results.md`](../spec/contracts/results.md) and
[`spec/contracts/terraform.md`](../spec/contracts/terraform.md). Every
identifier below is a placeholder.

## Console output

Headroom prints the SCP placement recommendations, then the RCP ones:

```
$ python -m headroom --config sample_config.yaml

================================================================================
SCP PLACEMENT RECOMMENDATIONS
================================================================================

Check: deny_ec2_ami_owner

  Recommended Level: ROOT
  Affected Accounts: 4
  Compliance (affected accounts): 100.0%
  Reasoning: All accounts in organization have zero violations - safe to deploy at root level
  --------------------------------------

Check: deny_rds_unencrypted

  Recommended Level: OU
  Target OU: high_value_assets (ou-1111-11111111)
  Affected Accounts: 2
  Compliance (affected accounts): 100.0%
  Reasoning: All 2 accounts under OU 'high_value_assets', including those in its child OUs, have zero violations - safe to deploy at OU level
  --------------------------------------

================================================================================
RCP PLACEMENT RECOMMENDATIONS
================================================================================

Check: deny_sts_third_party_assumerole

  Recommended Level: OU
  Target OU: acme_acquisition (ou-2222-22222222)
  Affected Accounts: 2
  Third-Party Accounts: 1
  Reasoning: OU 'acme_acquisition' with 2 accounts can be protected with OU-level RCP (allowlist contains 1 third-party accounts from union of account requirements)
```

A check with no safe placement is absent from the output and generates nothing.

## Generated Terraform

Every generated file opens with the ownership marker
`# Code generated by Headroom. DO NOT EDIT.` as its whole first line.
Reconciliation deletes marked files the current run did not plan, so a check
that stops being safe loses its statement without anyone editing the file.

### SCP module call

**Shape of**: `{scps_dir}/root_scps.tf`

```hcl
# Code generated by Headroom. DO NOT EDIT.
# Auto-generated SCP Terraform configuration for Organization Root
# Generated by Headroom based on compliance analysis

module "scps_root" {
  source = "../modules/scps"
  target_id = local.root_ou_id

  # EC2
  deny_ec2_ami_owner = true
  ec2_allowed_ami_owners = [
    "333333333333",
    "amazon",
  ]
  deny_ec2_imds_hop_limit = false
  deny_ec2_imds_v1 = false
  deny_ec2_public_ip = false

  # EKS
  deny_eks_create_cluster_without_tag = false

  # IAM
  deny_iam_saml_provider_not_aws_sso = false
  deny_iam_user_creation = true
  iam_allowed_users = [
    "arn:aws:iam::${local.fort_knox_account_id}:user/service/github-actions",
    "arn:aws:iam::${local.acme_co_account_id}:user/terraform-user",
  ]

  # Lambda
  deny_lambda_auth_type_none = false

  # RDS
  deny_rds_unencrypted = false
}
```

Two things to read here. All nine registered SCP checks appear, so `false`
records "scanned, not safe to enforce here" rather than "not considered". And an
allowlist is the union across every account the target covers — `iam_allowed_users`
names users in two accounts because the statement attaches at the root.

### RCP module call

**Shape of**: `{rcps_dir}/acme_acquisition_ou_rcps.tf`

```hcl
# Code generated by Headroom. DO NOT EDIT.
# Auto-generated RCP Terraform configuration for OU acme_acquisition
# Generated by Headroom based on third-party account analysis

module "rcps_acme_acquisition_ou" {
  source = "../modules/rcps"
  target_id = local.acme_acquisition_ou_id

  # ECR
  deny_ecr_third_party_access = false

  # KMS
  deny_kms_third_party_access = false

  # S3
  deny_s3_third_party_access = false

  # Secrets Manager
  deny_secrets_manager_third_party_access = false

  # SQS
  deny_sqs_third_party_access = false

  # STS
  deny_sts_third_party_assumerole = true
  sts_third_party_assumerole_account_ids_allowlist = [
    "111111111111",
  ]

  # Service confused deputy
  deny_service_confused_deputy = false
}
```

All seven registered RCP checks appear, for the same reason the SCP module names
all nine. An enabled RCP check carries its own allowlist variable; a disabled one
carries none.

### Organization data sources

**Shape of**: `{scps_dir}/grab_org_info.tf`

Headroom never writes an account ID or OU ID into the deployment Terraform. It
resolves each one by name through a data source, and guards the lookup so a
rename fails the plan instead of silently retargeting the policy:

```hcl
# Code generated by Headroom. DO NOT EDIT.
# Auto-generated Terraform configuration for AWS Organizations structure
# Generated by Headroom for SCP/RCP deployment targeting

data "aws_organizations_organization" "org" {}

data "aws_organizations_organizational_units" "root_ou" {
  parent_id = data.aws_organizations_organization.org.roots[0].id
}

data "aws_organizations_organizational_unit_child_accounts" "high_value_assets_accounts" {
  parent_id = local.high_value_assets_ou_id
}

locals {
  root_ou_id = data.aws_organizations_organization.org.roots[0].id

  validation_check_high_value_assets_ou = (length([for ou in data.aws_organizations_organizational_units.root_ou.children : ou.id if ou.name == "high_value_assets"]) == 1) ? "All good. This is a no-op." : error("[Error] Expected exactly 1 high_value_assets OU, found ${length([for ou in data.aws_organizations_organizational_units.root_ou.children : ou.id if ou.name == "high_value_assets"])}")

  high_value_assets_ou_id = [
    for ou in data.aws_organizations_organizational_units.root_ou.children :
    ou.id if ou.name == "high_value_assets"
  ][0]
}
```

The file is generated once, into the SCP directory, and shared by every module
call in both directories. The RCP directory does not get a second copy: it gets
a relative **symlink** to the SCP directory's file, created by
`ensure_org_info_symlink` in `headroom/main.py`.

That is load-bearing rather than tidy. Reconciliation deletes marked files the
current run did not plan, and a real second file would be a second thing for it
to claim. A symlink is never claimed, and the check happens before the file is
read. [`spec/contracts/terraform.md`](../spec/contracts/terraform.md) owns the
rule.

## JSON results

One file per check per account, named for the account. The `summary` object
carries account identity and the counts placement reads; the sibling keys carry
the evidence.

### An SCP result

**Shape of**: `{results_dir}/scps/deny_ec2_imds_v1/fort-knox.json`

```json
{
  "summary": {
    "account_name": "fort-knox",
    "account_id": "444444444444",
    "check": "deny_ec2_imds_v1",
    "total_instances": 1,
    "violations": 0,
    "exemptions": 1,
    "compliant": 0,
    "compliance_percentage": 100.0
  },
  "violations": [],
  "exemptions": [
    {
      "region": "us-east-1",
      "instance_id": "i-11111111111111111",
      "imdsv1_allowed": true,
      "exemption_tag_present": true
    }
  ],
  "compliant_instances": []
}
```

An exempt instance is not a violation, so this account is safe to cover — the
generated statement carries the matching exemption condition.

### An RCP result

**Shape of**: `{results_dir}/rcps/deny_sts_third_party_assumerole/acme-co.json`

```json
{
  "summary": {
    "account_name": "acme-co",
    "account_id": "222222222222",
    "check": "deny_sts_third_party_assumerole",
    "total_roles_analyzed": 1,
    "roles_third_parties_can_access": 1,
    "roles_with_wildcards": 0,
    "violations": 0,
    "unique_third_party_accounts": [
      "111111111111"
    ],
    "third_party_account_count": 1
  },
  "roles_third_parties_can_access": [
    {
      "role_name": "ThirdPartyVendorA",
      "role_arn": "arn:aws:iam::222222222222:role/ThirdPartyVendorA",
      "third_party_account_ids": [
        "111111111111"
      ],
      "has_wildcard_principal": false
    }
  ],
  "roles_with_wildcards": []
}
```

A third-party account here is a finding, not a violation: it becomes an entry in
the generated allowlist. A wildcard principal is the violation, because no
allowlist can preserve it.

## Directory layout after a run

```
test_environment/
├── headroom_results/
│   ├── scps/<check-id>/<account-name>.json
│   └── rcps/<check-id>/<account-name>.json
├── scps/
│   ├── grab_org_info.tf
│   ├── root_scps.tf
│   ├── high_value_assets_ou_scps.tf
│   └── acme_acquisition_ou_scps.tf
└── rcps/
    ├── grab_org_info.tf -> ../scps/grab_org_info.tf
    ├── acme_acquisition_ou_rcps.tf
    └── security_tooling_rcps.tf
```

One directory per registered check under `scps/` and `rcps/`, whether or not any
account produced a finding: a result file is written for every check on every
analyzed account, so the directory appears even when the check found nothing.
The Terraform files that exist are the ones a safe placement was found for, one
per target, which is why the two directories name different targets.

## Workflows

### Initial scan

```bash
python -m headroom --config config.yaml
git diff test_environment/headroom_results/
git add test_environment/ && git commit -m "Initial Headroom scan results"
```

### Periodic re-scan

```bash
python -m headroom --config config.yaml
git diff test_environment/headroom_results/   # new findings
git diff test_environment/scps/ test_environment/rcps/  # statements gained or lost
```

Committing the results is what makes the second run legible: the diff is the
compliance change.

### Deploying

```bash
python -m headroom --config config.yaml
cd test_environment/scps && terraform plan && terraform apply
cd ../rcps && terraform plan && terraform apply
```

## Complete examples

The repository's own live-test output is committed, and is real generator
output rather than an excerpt:
[`test_environment/scps/`](../test_environment/scps),
[`test_environment/rcps/`](../test_environment/rcps), and
[`test_environment/headroom_results/`](../test_environment/headroom_results).
See [`test_environment/README.md`](../test_environment/README.md) for what
produced them.

Two things to expect when you open them, both consequences of how they were
produced rather than of how the tool behaves today.

They are **older than the current registry**. A scan needs live AWS credentials
and costs real money, so these are not regenerated on every change. Sixteen
checks are registered — nine SCP and seven RCP — while the committed results
hold five SCP and three RCP check directories, and `scps/root_scps.tf` names six
of the nine. That is why the two module blocks above are labelled "shape of":
they show what today's generator emits, which is every registered check.

They were produced with `exclude_account_ids: true`, the setting
[`sample_config.yaml`](../sample_config.yaml) ships. Result filenames therefore
carry no account ID, the `summary` object has no `account_id` key, and account
IDs inside ARNs read `REDACTED`. The JSON above shows the un-redacted shape.
